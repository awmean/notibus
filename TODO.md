- [ ] do i really need `glib` to run that loop?
- [ ] one of the key ideas was to allow the notifications to be sent only as root -> need to create `.conf` file for dbus
  to allow everyone listen but only root send 